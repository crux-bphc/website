---
interface Props {
  style: string;
  text: string;
}

const { style, text } = Astro.props;
---

<span
  class=`rounded-lg h-fit font-sans sm:px-2 lg:px-3 px-2 text-xs sm:text-sm sm:py-1 py-[0.125em] ${style}`
  >{text}</span
>
