---
interface Props {
  style: string;
  text: string;
}

const { style, text } = Astro.props;
---

<span
  class=`rounded-lg font-sans sm:px-4 px-3 sm:text-md text-sm sm:py-1 py-[0.125em] ${style}`
  >{text}</span
>
